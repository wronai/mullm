pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
fastapi==0.104.1
starlette==0.27.0
# Starlette 0.27 + TestClient wymagają anyio 3.x; anyio 4.x z innego venv (np. koru) psuje backend asyncio.
anyio>=3.7.1,<4.0.0
