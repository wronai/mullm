[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = services/orchestrator
markers =
    integration: requires live Postgres (MULLM_INTEGRATION=1)
    e2e_live: requires running Mullm web stack (MULLM_E2E=1)
